sequence to make changes 

git add . 
git commit -m "your message here"
git push origin <branch_name> 

sequence to clone repo
===============================
git clone -b <branch_name> <repo link> <save location> 
cd into that dir 
log into git using the peronsal access token 
put that as the password when prompted in the terminal 